type Wallet @entity {
  id: ID!
  tokens: [TokenBalance!]!
}

type Token @entity {
  id: ID!
  symbol: String!
  name: String!
  decimals: BigInt!
}

type TokenBalance @entity {
  id: ID!
  balance: BigInt!
  token: Token!
}
